# RETIRED — wrong project (key-journal-378204).
# Use deploy-prod.yml / deploy-preview.yml for resumora-live instead.
name: Retired Hosting Workflow

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Point operators to the correct workflows
        run: |
          echo "This workflow is retired. Use:"
          echo "  .github/workflows/deploy-prod.yml"
          echo "  .github/workflows/deploy-preview.yml"
          echo "projectId must be resumora-live"
          exit 1
