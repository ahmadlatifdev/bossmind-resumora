# Stripe / billing secrets for Firebase Functions (resumora-live)
# Copy values from D:\BossMind\config\secrets.env or project .env — NEVER commit filled values.
#
# Prefer Firebase Secret Manager / params (Gen2) over legacy functions:config.
#
# Required for webhook + checkout + cancel/refund:
STRIPE_SECRET_KEY=sk_live_REPLACE_OR_USE_sk_test_
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_FROM_STRIPE_DASHBOARD_ENDPOINT_we_1U9Wy6KDmqaZUHNovW1Ac7YO
#
# Optional:
RESEND_API_KEY=
SENDGRID_API_KEY=
DUNNING_FROM_EMAIL=billing@resumora.net
ALLOW_UNAUTH_BILLING=false
#
# Local webhook server:
PORT=3005
REDIS_URL=
#
# After setting secrets on the project, redeploy:
#   npx firebase-tools deploy --only functions:resumora-checkout:stripeWebhook,functions:resumora-checkout:getRefundPreview,functions:resumora-checkout:cancelSubscription,functions:resumora-checkout:recordServiceEventHttp,functions:resumora-checkout:listRefundsHttp --project resumora-live
#
# Or set GitHub Actions secrets FIREBASE_SERVICE_ACCOUNT + STRIPE_* and merge to main.
