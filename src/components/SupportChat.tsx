/**
 * Support Chat — paid-member Client Chat (policy-driven backend).
 * Implementation lives in ChatWidget (includes View Payment History → /account#transactions).
 */
export { default } from './ChatWidget';
