# Skill: retrieve-video-assets (Hermes)

Route **library / manage videos / video assets** queries here — never to video-script-gen.

Runtime:

- Node: `functions/lib/skills/retrieve-video-assets.js` via `toolRouter`
- Python CLI: `skills/retrieve-video-assets/video_toolkit.py` → `GET /api/video/catalog`
- Admin UI: `/admin/videos` → `GET /api/admin/video-assets`
